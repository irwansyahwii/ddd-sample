import { RegisterUserAndVehicleDTO } from "./RegisterUserAndVehicleDTO";
import { User } from "../domain-models/User";

export class UserFactory{
    static CreateUserFromDTO(dto: RegisterUserAndVehicleDTO):User{
        throw new Error("");
        
    }
}